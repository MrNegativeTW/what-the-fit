package com.txwstudio.app.whatthefit.ui.components

import androidx.compose.ui.unit.dp

/**
 * Bottom inset for a scrollable list that has an overlaid bottom-end FloatingActionButton, so the
 * final item can scroll clear of the button. The value is the 56dp FAB size, its 16dp margin, and a
 * 16dp gap.
 */
internal val FabListBottomPadding = 88.dp
